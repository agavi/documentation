1) Command line examples should be wrapped in <screen>. The parts that are typed by the user are highlighted in the output and should be wrapped in <userinput>
2) Side notes should be placed in <note>
3) Tips should be placed in <note type="tip">
